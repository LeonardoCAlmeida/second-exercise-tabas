<div class="d-flex justify-content-center">
  <div>
    <% @properties.first(3).each_with_index do |pr, index| %>
      <%= render partial: 'card_property', locals: { property: pr, index: index } %>
    <% end %>
  </div>
</div>
<div class="d-flex justify-content-center pb-4">
  <%= link_to 'View more places', apartments_path(state_id: params[:state_id]), class: 'btn btn-secondary' %>
</div>
